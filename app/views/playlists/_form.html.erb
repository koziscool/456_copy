<% list = @playlist || Playlist.new %>
<%= form_for list do |f| %>
  <%= f.label 'Playlist Name: '%>
  <%= f.text_field :name %>
  <%= hidden_field_tag :user_id, current_user.id %>
  <%= f.submit %>
<% end %>
